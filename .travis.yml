sudo: 'required'

services:
  - 'docker'

language:
  - 'bash'

env:
  - ARCH=amd64
  - ARCH=arm32v6
  - ARCH=arm32v7
  - ARCH=arm64v8

jobs:
  include:
    - stage: build docker image
      script: echo "Building docker images for: ${ARCH}"
    - stage: test
      script: echo "Pushing docker images."
