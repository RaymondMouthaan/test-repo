sudo: 'required'

services:
  - 'docker'

language:
  - 'bash'

env:
    - ARCH=amd64
    - ARCH=arm32v6
    - ARCH=arm32v7
    - ARCH=arm64v8

# Build
script: echo "Arch ${ARCH}"

jobs:
    include:
        - stage: push
          script: echo "Arch ${ARCH}"
